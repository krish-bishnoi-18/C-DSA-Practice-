#include <stdio.h>

int main() {
    int arr [5],max=0;
    for (int i = 0; i < 5; i++)
    {
        scanf("%d",&arr[i]);
    }
    int maxnumber=arr[0];
    for (int i = 0; i < 5; i++)
    {
        if (arr[i]>maxnumber)
        {
        maxnumber=arr[i];
         max=i;
        } 
    }
    printf("%d is maxnumber and it is place at postion  %d  ",maxnumber,max);
    return 0;
}
