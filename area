#include <stdio.h>

float areaOfRectangle(float length, float width) {
    return length * width;
}

int main() {
    float l, w;
    printf("Enter length and width: ");
    scanf("%f %f", &l, &w);
    printf("Area = %.2f\n", areaOfRectangle(l, w));
    return 0;
}
